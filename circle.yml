# NOTE: Put any CircleCI overrides in this file.
# Add Chrome Monitor (https://chrome.google.com/webstore/detail/circleci-monitor/pkdkfaokllclembpkggkndfejdhcpmpk?hl=en)
# https://circleci.com/docs/manually
# https://circleci.com/docs/language-python

# https://circleci.com/docs/docker
machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t richburdon/dark-zero .
